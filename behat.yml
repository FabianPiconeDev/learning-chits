default:
    suites:
        default:
            contexts:
                - FeatureContext:
                    commandBus:   '@tactician.commandbus'
    extensions:
        Behat\Symfony2Extension: ~